{
  "name": "destinyone-sites-preview",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"require('fs').accessSync('dist/index.html'); console.log('Using verified Expo web export')\"",
    "build:web": "npm run build"
  }
}
